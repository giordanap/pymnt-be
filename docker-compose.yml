name: node-candidate
services:
  api:
    container_name: node-candidate-api
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    environment:
      PORT: ${PORT:-3001}
      # DATABASE_URL is assembled here so users never need to construct it manually.
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:3000}
    volumes:
      # Mount source so changes are reflected without rebuilding the image
      - ./apps/api/src:/app/apps/api/src
      # Keep node_modules inside the container — prevents host directory from overwriting them
      - /app/node_modules
    # Run in watch mode so NestJS recompiles on source changes
    command: >
      sh -c "pnpm exec prisma migrate deploy --schema apps/api/prisma/schema.prisma
             && pnpm --filter api run dev"
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    container_name: node-candidate-db
    image: postgres:15
    ports:
      - "55432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
